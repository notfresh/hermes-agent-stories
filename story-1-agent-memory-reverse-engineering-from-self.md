# 智能体记忆：一场以自身为样本的逆向工程

> 播客口播稿

## 前言

今天我们来聊一个话题：智能体的记忆。

不过在开始之前，我想先说一句——这篇文章不是我翻论文翻出来的，也不是我凭空设计的。它是一次逆向工程，样本是我自己。

我观察自己怎么定位一个模块、怎么猜一块代码是干什么的、怎么在久不接触之后只剩一个轮廓、又怎么在需要的时候快速把细节捡回来。然后我试着把这些真实发生的行为，推演成一套智能体记忆的设计。

它还没有跑通，还只是一个从自身经验里推出来的框架。但我觉得，它比凭空设计一套架构，更接近记忆本来的样子。

那下面，就是我梳理出来的框架。

---

## 一、记忆、知识、写死的东西，是三回事

我们先从边界开始。

记忆，是关于这个项目、这段经历、这个上下文的。它会模糊、会衰减、会重构。而知识，是通用的、稳定的、可复用的——比如 Java 语法怎么写，什么是高内聚低耦合。

这两者必须分开。混在一起，记忆系统就会变成一个什么都往里塞的垃圾场，检索的时候既不准也不快。

还有第三类东西：写死的。比如人的膝跳反应，比如一个固定数据的查询。这些是代码层面的，是确定性的，根本不该作为记忆存在。它们不需要衰减，不需要重载，也不需要模糊。把它们塞进记忆，纯属浪费。

所以你看，记忆是活的，知识是稳的，写死的是死的。三个东西，三种处理方式。

---

## 二、记忆的载体是连接，不是节点

接下来是我最早想清楚的一点：记忆不在单个节点上，而在节点之间的连接模式里。

就像神经网络，知识不在某一个神经元里，而在权重和连接模式里。所以"记住"不是往某个地方塞一条记录，而是调整节点之间的连接强度；"遗忘"也不是删除某条数据，而是连接衰减到不再被激活。

那连接从哪来？靠因果、类比、目标相关性。不能只靠共现——两个词总一起出现就连起来，那记住的只是统计关联，不是理解。冰淇淋和溺水总一起出现，但真正的原因是夏天。

而删除连接，不是 bug，是特征。一个全连接的记忆网络等于没有记忆，因为什么都关联，等于什么都检索不出来。遗忘，其实是记忆能工作的前提。

---

## 三、记忆挂在职责上，不挂在文件上

那节点应该是什么呢？我的答案是：职责，不是文件路径。

为什么？因为人关心的是用途，是价值。一个模块是干什么的、解决什么问题的，这个东西人记得最深。文件路径只是地址，地址会变——文件会重构、会改名、会拆分——但职责相对稳定。

所以记忆应该挂在职责、概念上，文件路径只是它当前的一个地址。地址变了，记忆不该丢，重新绑定就行。这也呼应了上一条：连接比节点重要，而职责，就是那个最稳定的连接锚点。

---

## 四、记忆分层，判据是重建成本和理解依赖度

记忆不是平的，它分层。而分层的判据，不是简单的"最近有没有用"，而是重建成本和理解依赖度。

我把它分成三层。

**锚点层**：项目是做什么的、解决什么问题。这一层几乎永驻。比如 Hermes Agent，我知道它是做什么的，这就是一个锚点记忆。它一旦丢了，整个记忆网络就失去了组织中心。

**结构层**：源码布局、各模块职责。这一层重要，但可以重载。用多了就深，用少了就淡。某个源码文件是干啥的、项目的源码布局是什么，都属于这一层。

**细节层**：某个方法具体怎么写、用的是哪个 Java 版本。这一层价值低、查询频率高，持久化到磁盘，用到才热，不用就衰减。很多时候，只有真正用到，才会去查。

判据很清楚：重建不了的，常驻；随时能翻代码重建的，可以卸载。

---

## 五、渐进式披露，是执行效率的原则

好，接下来是一条原则：记忆一定要渐进式披露。这事关执行效率。

什么意思？就是不预先加载全部。先拿最粗的锚点，按需往下钻，钻到哪层算哪层。

那怎么精准找到有价值的记忆？先激活锚点，顺着连接扩散，按清晰度排序——清晰度高的直接用，清晰度低的地方触发重载。

这就像人翻开笔记本，不会从头读到尾，而是直接翻到相关那页，快速捡起需要的东西。

---

## 六、模糊，是记忆的本质属性

下面这一点，我觉得是整套设计里最关键的：记忆一定是模糊的，不停变化的。它和知识不一样——知识要精确，记忆恰恰要模糊。

模糊有两层含义。

**第一层，是概率性。** 人脑子里很多东西是记不清楚的，只有一个大概，一个概率性的印象，然后靠推测去补。不是精确存储"feishu.go 第 120 行有个 connect 方法"，而是存一个带不确定性的印象："飞书连接应该在某个 client 模块里，具体哪个文件记不太清，但大概是处理鉴权和长连接那块。"回忆不是读取，是带先验的搜索。

**第二层，是时间衰减。** 越近用的，越明亮、越确定；隔得越久，越模糊、越只剩轮廓。清晰度，是时间的函数。

合起来就是：清晰度 = f(最近使用时间, 使用频率, 与锚点的连接强度)。它不是布尔值，是连续的置信度。

模糊会带来两个副产品。

一是记忆自带"可信/待验证"标签。清晰度高的直接拿来用，清晰度低的用的时候自动触发验证。Agent 不需要外部告诉它这条记忆靠不靠谱，记忆自己带着信号。

二是清晰度就是重载成本预估。清晰度低，重载成本就高，那要么现在花代价重载，要么先绕开。这就把记忆和任务规划接上了：接到任务先估哪里熟哪里生，就是拿清晰度当成本表用。

还有一点很重要：衰减是有梯度的。细节先掉，轮廓后掉。因为细节依赖的节点多、连接弱，一衰减先散；轮廓挂在锚点上，连接强，撑得久。所以一个久未触碰的模块，你记得的往往是"它负责用户鉴权"，而不是"它有个 validateToken 方法"。越老越抽象，但不会越老越没用。轮廓优先，因为轮廓，是重载的入口。

---

## 七、快装、快卸、半衰期，是一对工程化能力

再往下，是一组工程化的能力。

人类不能快速卸载记忆，只能靠自动衰减，慢慢忘。这是人的缺陷，不是优点。Agent 没理由继承这个缺陷。

所以 Agent 应该同时具备两种能力。

**快速卸载**：上下文切换、任务结束时，主动清掉不再需要的记忆，腾出热记忆空间。不用等它自然衰减。

**快速装载**：模糊到一定程度，触发重载——查代码、问 AI、翻文档，把模糊重新变清晰。人靠笔记本快速捡起东西，这就是记忆的重载。

而**半衰期**，负责长期自动衰减：没用到的慢慢衰减，用到的就记住。给每条记忆加一个半衰度。而锚点记忆的半衰期应该是无穷大，或者极慢——它不能丢。

三者配合：半衰期管长期，快装快卸管即时。自动衰减负责清理，主动卸载负责腾挪，快速重载负责捡回。

---

## 八、主动重构，是仿生的夜间 GC

这套机制里，我觉得最仿生的一点，是主动重构。

白天增量写入，夜间离线整理。人睡觉的时候，大脑会自动运行，把白天的经历重放一遍，判断哪些重要、哪些不重要，重排一次，顺手把垃圾回收掉。

Agent 也应该这样。任务结束后，不是简单写一条日志，而是把这次经历和已有的记忆节点重新连线。比如这次调查了飞书连接，可能同时强化了"项目鉴权模块""长连接处理""编译测试流程"好几条连接。下次再碰类似问题，激活的不只是单点，而是一整片相关区域。

这就是"连接慢慢建立"的真正含义：不是线性追加，是网状重组。重排，判断轻重，垃圾回收。

---

## 九、自维持循环

把这些串起来，就是一个自维持的循环。

清晰度随时间衰减，随调用增强。模糊到一定程度触发重载，重载后重新变清晰。每次重载又重塑连接。白天的实时读写，夜间的离线整理，半衰期的长期衰减，快装快卸的即时腾挪——它们共同维持着这个生态。

---

## 十、一个真实例子：调查飞书连接

抽象说了这么多，我们回到具体。假设我要调查 Hermes Agent 是怎么和飞书连接的。我的真实过程是这样的。

先定位。我大概知道这个功能在哪一块，但不确定。于是我去看项目文档结构——这是一条很重要的记忆，它告诉我各个源码的职责，让我知道去哪找。这是索引记忆。

然后跳到对应的源码。我用软件工程思维去猜：哪一块是哪一块，哪个方法是做什么的，有没有高内聚低耦合，每个方法的职责是什么。这是重构式回忆——不是精确读取，是边猜边验证。

有时候我觉得这样太慢，就让 AI 帮我快速扫描一下，然后把结果固化下来。这就是记忆写入，把临时的、外部的理解变成自己的。

摸清原理之后，改代码，编译，运行测试。大概就是这么一个过程。

而"我如何增加一个特性"，本身也是一条记忆：定位、摸清原理、改代码、编译、测试。下次再做，大概还会这么走。这就是流程记忆的复用。

如果把这个过程对应到前面的框架：文档结构是索引记忆；猜测是重构式回忆；AI 扫描后固化是记忆写入；编译测试通过是记忆强化。整条链路，就是渐进式披露的落地——先拿最粗的锚点，再按需往下钻，钻到哪层算哪层，不预先加载全部。

---

## 尾声

好，这套从我自己身上推出来的框架，基本上就梳理到这儿了。但还有两个问题，我到现在也没想透。

第一，重载时机。 重载太勤，等于没记忆；重载太懒，等于用错记忆。这个阈值，可能是 Agent 记忆智能与否的分水岭。

第二，模糊的工程实现。 是存一个带置信度的摘要？还是存多个版本的碎片，回忆时重新拼？还是连接权重本身就能表达模糊？

这两个问题，就留给下一步吧。